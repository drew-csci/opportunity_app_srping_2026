# Opportunity App

## Project Description

The Opportunity App is a Django-based web application designed to facilitate the management of volunteer opportunities, student achievements, and related activities for students, organizations, and administrators. Built using Python and Django framework, it provides a user-friendly interface for registration, login, and role-based access to different functionalities.

## Project Purpose

The primary purpose of the Opportunity App is to create a centralized platform where:

- Students can register, log in, and track their achievements and volunteer opportunities.
- Organizations can manage and view relevant information related to opportunities.
- Administrators can oversee the entire system, manage users, and ensure smooth operation.

The app aims to streamline the process of connecting students with volunteer opportunities while maintaining a secure and organized database of user activities.

## File and Folder Structure

```
opportunity_app_srping_2026/
├── bootstrap.py
├── manage.py
├── output_20251011.xml
├── pack_dir_to_xml.py
├── Readme.md
├── Readme_NguyenTinTin_Do.md
├── requirements.txt
├── test.py
├── accounts/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── forms.py
│   ├── models.py
│   ├── tests.py
│   ├── urls.py
│   ├── views.py
│   ├── __pycache__/
│   └── migrations/
│       ├── __init__.py
│       ├── 0001_initial.py
│       └── __pycache__/
├── opportunity_app/
│   ├── __init__.py
│   ├── asgi.py
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── __pycache__/
├── pages/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── forms.py
│   ├── models.py
│   ├── tests.py
│   ├── urls.py
│   ├── views.py
│   ├── __pycache__/
│   └── migrations/
│       ├── __init__.py
│       ├── 0001_initial.py
│       └── __pycache__/
├── static/
│   └── css/
│       └── styles.css
└── templates/
    ├── base.html
    ├── accounts/
    │   ├── login.html
    │   └── register.html
    └── pages/
        ├── dashboard.html
        ├── faq.html
        ├── screen1.html
        ├── screen2.html
        ├── screen3.html
        ├── student_achievements.html
        ├── welcome.html
        └── partials/
            ├── s1_administrator.html
            ├── s1_organization.html
            ├── s1_student.html
            ├── s2_administrator.html
            ├── s2_organization.html
            ├── s2_student.html
            ├── s3_administrator.html
            ├── s3_organization.html
            └── s3_student.html
```

## Description of Each Important File

### Root Level Files

- **bootstrap.py**: A setup script that automates the project initialization, including creating virtual environments, installing dependencies, setting up the Django project, configuring the database, and generating basic templates.
- **manage.py**: Django's command-line utility for administrative tasks such as running the development server, applying database migrations, and creating superusers.
- **output_20251011.xml**: An XML file generated by `pack_dir_to_xml.py` containing a packaged representation of all project files.
- **pack_dir_to_xml.py**: A utility script that scans the project directory and packages all files into an XML format for archival or transfer purposes.
- **Readme.md**: The primary README file containing setup instructions and basic project information.
- **requirements.txt**: A text file listing all Python dependencies required for the project, installable via `pip install -r requirements.txt`.
- **test.py**: A test script that generates sample HTML files for different user types and screens for testing purposes.

### Important Folders

- **accounts/**: Handles user authentication and management, including registration, login, and user profile data.
- **opportunity_app/**: The main Django project directory containing global settings, URL configurations, and deployment files.
- **pages/**: Manages the application's main pages and business logic, including views for different screens and user interactions.
- **static/**: Contains static files such as CSS stylesheets that are served directly to the client.
- **templates/**: Holds HTML templates used to render the web pages, organized by app and functionality.

## How the Files Interact

The Django framework orchestrates the interaction between files:

1. **Entry Point**: `manage.py` serves as the entry point, loading settings from `opportunity_app/settings.py` and executing commands.
2. **URL Routing**: `opportunity_app/urls.py` defines the root URL patterns and includes URL configurations from `accounts/urls.py` and `pages/urls.py`.
3. **Views and Logic**: View functions in `accounts/views.py` and `pages/views.py` handle HTTP requests, interact with models in `accounts/models.py` and `pages/models.py` to retrieve or store data in the database, and render appropriate templates.
4. **Templates and Static Files**: Views pass data to HTML templates in the `templates/` directory, which extend `base.html` for consistent layout. Static files like CSS are linked from the `static/` directory.
5. **Database Interaction**: Models define the database schema, and migrations in the `migrations/` folders ensure the database structure matches the model definitions.
6. **Authentication**: The `accounts` app manages user authentication, with middleware in `opportunity_app/settings.py` ensuring authenticated access to protected views.

## How to Run the App

1. **Prerequisites**: Ensure Python 3.8+ is installed on your system.
2. **Clone or Download**: Obtain the project files.
3. **Set up Virtual Environment**:

   ```bash
   python -m venv venv
   # On Windows:
   venv\Scripts\activate
   # On macOS/Linux:
   source venv/bin/activate
   ```
4. **Install Dependencies**:

   ```bash
   pip install -r requirements.txt
   ```
5. **Configure Environment**: Set up environment variables for database connection and Django settings (refer to `opportunity_app/settings.py` for required variables).
6. **Run Migrations**:

   ```bash
   python manage.py migrate
   ```
7. **Create Superuser** (optional):

   ```bash
   python manage.py createsuperuser
   ```
8. **Run the Development Server**:

   ```bash
   python manage.py runserver
   ```
9. **Access the Application**: Open a web browser and navigate to `http://127.0.0.1:8000/`.

## Summary

The Opportunity App is a comprehensive Django web application that provides a platform for managing student volunteer opportunities and achievements. It features role-based access for students, organizations, and administrators, with a clean separation of concerns across different Django apps. The project demonstrates best practices in Django development, including proper URL routing, model-view-template architecture, and database management. With its modular structure and clear documentation, it serves as an excellent example for learning Django web development and can be easily extended with additional features.
